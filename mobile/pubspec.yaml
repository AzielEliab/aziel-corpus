name: azielcorpus
description: Aziel Digital Library v2.7.0 mobile stub. Not a 26-card index.
publish_to: "none"
version: 2.7.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
