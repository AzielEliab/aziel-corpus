# Contributing

Forks are welcome and always allowed.

This is a public library index of Aziel Eliab software. Keep the honest
scope. Do not turn it into a private-file search engine, Zenodo, or a new
Lock engine. Public identity is Aziel Eliab only.

```bash
python -m venv .venv && source .venv/bin/activate && pip install -e ".[dev]"
pytest
aziel-corpus doctor
```
